# interview.md —— 烛照项目 · 面试口播稿

> 建立 2026-09-20 ｜ 配套代码：`src/`（已重构完毕，`nmake` 编译通过，产物 `src/bin/ZhuZhaoGUI.exe`）
>
> **这份文件是学习主线，`LEARNING_PROGRESS.md` 不再推新格子。**
>
> **它不是题库，是口播稿。** 面试官不会问你十几个问题，他会挑三四条顺着往下聊。
> 所以每条简历只准备 **3 句话**——3 句话能顺口说出来，这条就过关；细节等被追问了再补。

---

## 怎么用

**目标：合上文件，30 秒内把一条讲完。**

- 讲不顺 → 对着代码把那段读一遍，再讲
- 讲顺了 → 找大肥鲸模拟追问，顶得住就打 ✅

| 标记 | 含义 |
|---|---|
| ⬜ | 还没练过 |
| 🔶 | 能开头，追问一层就断 |
| ✅ | 能顺口讲完，追问也顶得住 |

## 三条通用铁律

1. **承认不会 > 编造。** 答不上就说「这块我没细究，但我知道方向是……」，好过胡诌被追问穿。
2. **数字要能自证。** 没实测过的耗时、精度、提升比例，一个都别写进简历。
3. **每个「我做了 X」配一句「当时遇到 Y」。** 没有困难的经历听起来像抄来的。

---

# 第 1 条 · 光度立体算法

**简历说的**：实现光度立体算法，建立光照方程，最小二乘求法向量与反照率，频域积分重建高度图。

### 三句话

**1｜这算法干什么**
拿 4 张打不同方向光的灰度图，反推出物体表面的形状（高度）和材质（反射率）。

**2｜怎么算出来的**
每张图每个像素满足 `I = ρ(N·L)`——亮度 = 反射率 × 法向量点乘光源方向。
4 张图在同一个像素给出 4 个方程，未知量只有 3 个（法向量的三个分量），方程比未知量多，属于超定，所以用最小二乘解。
矩阵是 4×3 不是方阵、不能直接求逆，用 SVD 求伪逆。**解出来的那个向量，长度是反射率，方向就是法向量。**

**3｜怎么变成能看的高度图**
有了法向量就能算出表面梯度。梯度积分成高度这一步我没有在空域逐点累加（那样误差会越滚越大），
是在**频域**里一次性积出来的：傅里叶变换到频域 → 做一次除法 → 逆变换回来。

### 追问保险

| 追问 | 一句话回答 |
|---|---|
| 至少几张图？ | 3 张。3 个未知量、3 个方程；4 张是多一个方程用来抗噪 |
| 这方法是你想的吗？ | 不是，经典光度立体方法，我按课程实现并逐行理解了 |
| 朗伯体假设的局限？ | 只假设漫反射。现实有高光、阴影、互反射，我没做鲁棒处理，知道的方向是加权最小二乘或 RANSAC |

**细节翻这里**：`src/PhotometricStereo/PhotometricStereo.cpp`（主函数 `:59`、SVD `:117`、频域积分 `:14-55`）

**动手**：跑一次 `ExampleMain`，数据集换成 `images/tire_`（角度 `{180,270,0,90}` / Slant 全 45），看高度图的凹凸方向对不对。

---

# 第 2 条 · 动态库分层

**简历说的**：算法封装为 CMake 构建的独立动态库，导出宏暴露单一函数接口，上位机以 qmake 构建并动态链接。

### 三句话

**1｜怎么分的**
算法整个抽成独立的 DLL，两边只通过**一个导出函数**通信。上位机不关心算法内部实现；
算法库不依赖 Qt，能被别的程序直接复用——`ExampleMain.exe` 就是第二个使用方。

**2｜导出宏怎么工作**
头文件里 `ZZ_API` 展开成 `dllexport` 还是 `dllimport`，看编译时有没有定义 `ALGO_EXPORT`。
**这个宏不是代码里 `#define` 的，是 CMake 通过 `target_compile_definitions` 传给编译器的**——编 DLL 时带上，用 DLL 时不带。

**3｜两套构建体系各管什么**
前端 qmake（`.pro`），算法库 CMake，产物都落到同一个 `src/bin`。
链接期用 `.lib`（导入库），运行期用 `.dll`。

### 追问保险

| 追问 | 一句话回答 |
|---|---|
| 缺 `.lib` 或 `.dll` 分别会怎样？ | 缺 `.lib` 链接期就报错；缺 `.dll` 能编过，启动时报找不到 DLL |
| 为什么不加 `extern "C"`？ | 它唯一的作用是关掉 C++ 名字修饰，而我的接口全是 C++ 类型（`vector`/`Mat`），C 链接表达不了，加了没意义 |
| 跨 DLL 传 `std::vector` 有风险吗？ | **有**。STL 对象在一侧的堆上分配、另一侧的堆上释放，CRT 不一致就会堆损坏。我知道这个隐患、当前没解决，正规做法是接口改成 C 风格的裸指针 + 长度 |

**细节翻这里**：`src/PhotometricStereo/PhotometricStereo.h:11-17`（导出宏）、`src/PhotometricStereo/CMakeLists.txt`

**加分项**（讲出来很亮）：源工程用的是目录级 `add_compile_definitions(ALGO_EXPORT)`，会让使用方也拿到这个宏、导出方向反过来。我改成了 `target_compile_definitions(... PRIVATE)`。

---

# 第 3 条 · Graphics/View 图像控件

**简历说的**：基于 Graphics/View 自建图像显示控件，滚轮缩放、双击自适应、鼠标像素与 RGB 实时显示；缩略图用 QListWidget + setItemWidget 绑自绘控件。

### 三句话

**1｜三层各管什么**
`QGraphicsScene` 管坐标系和元素容器，`QGraphicsView` 管视口和交互（缩放、平移），`QGraphicsItem` 是内容本身。
不用 QLabel 是因为要自己控制缩放锚点、坐标映射和多元素叠加。

**2｜缩放和自适应怎么做的**
滚轮事件里按 `angleDelta` 的正负决定放大还是缩小，每次乘 1.1 或 0.9，用一个成员变量记当前总倍率来卡上下限。
双击和窗口尺寸变化都触发 `fitFrame()`：按图像与窗口的比例算出该缩多少，补上差额，再居中。

**3｜像素坐标和 RGB 怎么来的**
鼠标移动事件里拿到的是**元素局部坐标**——Qt 已经帮我把 view→scene→item 三级变换算完了，所以直接就是像素下标，
拿去 `pixmap` 里取颜色，通过信号发给左下角标签显示。

### 追问保险

| 追问 | 一句话回答 |
|---|---|
| 缩略图那套怎么接的？ | 每个 `QListWidgetItem` 配一个自绘控件，用 `setItemWidget` 绑上去；控件重写 `paintEvent` 画图像和选中红框 |
| 背景为什么要自己画？ | 用 `setBackgroundBrush` 的话棋盘格会跟着图像一起缩放，看着像图像的一部分 |
| 两个缩略图列表代码几乎一样，怎么看？ | 只差三处配置（排列方向、滚动条策略、格子尺寸算法），抽基类反而更绕。这是取舍，不是没发现 |

**细节翻这里**：`src/ZhuZhaoGUI/ZZViewWidget/CustomGraphicsView.cpp`（滚轮 `:103`、自适应 `:163`）、`ZZThumWidget/HThumbnailList.cpp:75`、`ZZThumWidget/LitImgItemWidget.cpp:28`

**动手**：把 `CustomGraphicsView.cpp` 里 `fitFrame()` 末尾那行 `m_dZoomValue = target;` 注释掉，编译运行，滚轮缩放看有什么变化。

---

# 第 4 条 · 观察者模式

**简历说的**：观察者模式实现事件驱动，消息枚举 + 抽象监听基类，全局单例注册与广播，主窗口集中响应。

### 三句话

**1｜为什么不用 Qt 信号槽**
信号槽是点对点的，谁连谁得写清楚。我这里是一对多广播 + 接收方集中在主窗口处理，
避免子控件互相持有指针连成一张网。代价是消息退化成 int，没有类型检查。

**2｜怎么工作的**
抽象基类 `ZZListener` 只有一个纯虚函数 `RespondMessage`。谁想收消息就继承它，
在构造函数里调 `registerMessage(消息, this)` 把自己登记到单例的名单上。
任何地方调 `notify(消息)`，单例查出登记过这条消息的人，挨个调它们的 `RespondMessage`。

**3｜消息编号为什么必须是 2 的幂**
因为要用**位或**一次登记好几条（比如「输入图变了、结果出来了，我都要管」），再用**位与**把混在一起的消息拆回单条。
不是 2 的幂位运算就会串门——源工程 `RESET` 写成 3 就踩了这个坑，登记 `1|2` 的人会被误注册成 RESET 的监听者。

### 追问保险

| 追问 | 一句话回答 |
|---|---|
| 项目里有几个单例？ | 两个，正好演示两种写法：`ListenerManger` 饿汉式（启动就建、天生线程安全），`ZZLogMessage` 懒汉式 + 双检锁（第一次用才建） |
| 简历里「解耦界面与业务逻辑」准确吗？ | **不准确，我改一下**——它解耦的是**各控件之间**；项目里没有业务层，算法被隔在 DLL 里 |
| 监听者多了会怎样？ | 广播是同步遍历，一个慢全体都慢；消息是裸 int，带不了参数 |

**细节翻这里**：`src/ZhuZhaoGUI/ZZListener.h:24-30`（枚举）、`ZZListener.cpp:17-39`（notify）、`:41-75`（registerMessage）、`MainWindow.cpp:118-155`（集中响应）

---

# 第 5 条 · QThread 多线程

**简历说的**：QThread 子线程执行算法，主线程只做参数组织与界面刷新，完成后经 finished 信号回调更新结果区。

### 三句话

**1｜为什么要开线程**
算法是纯计算，放主线程里跑界面就卡死了（不能重绘、不响应点击）。
挪到子线程后，主线程只负责收参数、起线程，以及拿到结果后刷界面。

**2｜线程怎么写的**
继承 `QThread` 重写 `run()`，算法调用整个在 `run()` 里执行——也就是在子线程里。
参数用 `Set` 塞进去、结果用 `Get` 取出来，两边都过同一把互斥锁，因为主线程和子线程会同时碰这块成员。

**3｜结果怎么回到界面的**
`run()` 返回后 `QThread` 自动发 `finished` 信号，主窗口连了这个信号，槽里把结果取出来刷新缩略图。
**槽函数跑在主线程**——Qt 的规矩是槽永远在「接收者所属的那条线程」执行；接收者是主窗口，跨线程时连接会自动转成排队投递。

### 追问保险

| 追问 | 一句话回答 |
|---|---|
| 子线程里能直接改 UI 吗？ | 不能。Qt 控件不是线程安全的，必须把结果通过信号送回主线程 |
| 连点两次 RunOnce 会怎样？ | `start()` 对正在运行的线程是空操作，不会起第二条；参数会被第二次的覆盖（有锁，不会数据损坏） |
| 算法只跑几十毫秒，真需要开线程吗？ | （**先把实测耗时补上再讲这句**）即使当前够快，把计算移出 UI 线程也是可扩展性考虑——图像尺寸上去就会卡 |
| 想中途取消怎么做？ | 需要一个原子标志位在算法循环里检查；但算法库是黑盒没有回调点，要么改库接口，要么只能等它跑完 |

**细节翻这里**：`src/ZhuZhaoGUI/ZZConfigWidget/ZZProcessThread.cpp:33`（run）、`MainWindow.cpp:69-72`（连 finished）、`:109-116`（刷新）

---

# 第 6 条 · 日志系统

**简历说的**：MessageHandler 接管 Qt 全局消息输出，分级着色显示、按日期落盘、按大小自动归档。

### 三句话

**1｜为什么不直接往控件里写**
用 `qInstallMessageHandler` 把 Qt 的全局日志出口接管了。之后**任何地方**调 `qDebug/qWarning/qCritical` 都自动进这套系统，
业务代码里一行日志代码都不用写。**最大的收益是 Qt 框架自己产生的日志（QSS 解析失败之类）也会流进来。**

**2｜一条日志走三个出口**
按级别着色显示在面板上、追加写进 `log/日期/log.txt`、超过 1MB 改名归档成 `log_N.txt`。
落盘那段加了互斥锁，因为日志可能从任意线程打出来。

**3｜跨线程怎么保证安全**
日志从子线程发出时，`emit` 出去的信号被 Qt 自动转成**排队投递**，`append` 最终在主线程执行，所以不会崩。
另外 `emit` 必须放在锁外面——同线程直连时它是同步调用，放在锁里会自己等自己、死锁。

### 追问保险

| 追问 | 一句话回答 |
|---|---|
| 源工程这段有什么问题？ | 三处：单例里用局部 `QMutex` 导致双检锁失效；轮转的大小判断写在 `close()` 之后、取到不可靠的值；跨线程连接没显式处理。**都修了** |
| 双检锁现在还有必要吗？ | ⭐ 说实话**没必要**。C++11 之后静态局部变量初始化就是线程安全的，写 `static ZZLogMessage instance;` 更短更稳。保留双检锁是为了跟课程写法对齐 |
| 日志文件落在哪？ | exe 所在目录下 `log/日期/log.txt`。源工程用相对路径，会跟着启动方式到处跑（Qt Creator 里跑就落到构建目录） |

**细节翻这里**：`src/ZhuZhaoGUI/ZZLogWidget/ZZLogMessage.cpp:71`（接管函数）、`:121-127`（轮转）、`:148-172`（单例）、`ZZLogWidget.cpp:100`（上屏）

**动手**：把 `kMaxLogFileSize` 改成 `1024`，跑几次让日志连续触发轮转，看 `log_1.txt` 怎么出现的。

---

# 进度

6 条 × 3 句 = **18 个点**。不是背，是能顺口说出来。

| 条 | 内容 | 状态 |
|---|---|---|
| 1 | 光度立体算法 | ⬜ |
| 2 | 动态库分层 | ⬜ |
| 3 | Graphics/View 控件 | ⬜ |
| 4 | 观察者模式 | ⬜ |
| 5 | QThread 多线程 | ⬜ |
| 6 | 日志系统 | ⬜ |

## 8 天怎么排

| 天 | 做什么 | 达标 |
|---|---|---|
| D1 | 第 1 条 算法 | 三句话讲顺 + 追问保险过一遍 |
| D2 | 第 3 条 Graphics/View | 同上 |
| D3 | 第 5 条 QThread | 同上 |
| D4 | 第 4 条 观察者 | 同上 |
| D5 | 第 2 条 动态库 + 第 6 条 日志（这两条最短，并成一天） | 两条都过 |
| D6 | 全量对练第一轮：随机抽条，大肥鲸追问两层 | 6 条都顶得住 |
| D7 | 简历定稿 + 对练第二轮（专挑前一天答断的） | 改完读一遍顺不顺 |
| D8 | 模拟面试：从自我介绍聊到项目 | 全程不用看文件 |

**为什么 8 天够**：每条只有 3 句话，一天一条是宽裕的。真正花时间的不是背，是**被追问时怎么圆**——
所以 D6、D7 两轮对练不能省，那是把「知道」变成「说得出」的地方。

**时间还不够就砍**：优先保 **第 1 条（算法）、第 3 条（Graphics/View）、第 5 条（多线程）**——这三条面试官问得最多。
第 2 条动态库和第 6 条日志讲得浅一点不致命。

---

# 简历措辞改稿清单

> ⚠️ **这不是题目，跟上面那 6×3 句话没关系。**
> 它是**简历原文里要换掉的几个词**——左边是现在写的，右边是建议改成的。
> 为什么非换不可：左边那些说法面试官一听就想追问，而你手上没有对应的东西可以答，
> 一句话就把整份简历的可信度拉低了。

| # | 现在的问题 | 改成 |
|---|---|---|
| 1 | 「前后端分离架构」是 Web 术语 | 「UI 与算法分层解耦」 |
| 2 | 「实现事件驱动架构」夸大 | 「以观察者模式做控件间解耦」 |
| 3 | 「避免界面卡顿」是结果宣称 | 「把计算移出 UI 线程」 |
| 4 | 「解耦界面与业务逻辑」主语错了 | 「解耦各界面控件之间的耦合」 |
| 5 | 整段像「我发明了这套算法」 | 保留「实现」这个词，但要准备好「是不是你有改进」的回答 |

---

# 面试前 10 分钟速查

1. **一条主线**：4 张不同光照的灰度图 → 光照方程 → SVD 解出法向量和反射率 → 梯度场 → 频域积分 → 高度图
2. **一条数据流**：点 RunOnce → `notify` → 主窗口响应 → 子线程跑算法 → `finished` → 刷新结果图
3. **一处最得意的修正**：`add_compile_definitions` 改成 `target_compile_definitions(... PRIVATE)`——宏传错作用域会让使用方也按 dllexport 编译
4. **一处最诚实的短板**：跨 DLL 传 STL 对象的 CRT 风险，我知道、没解决、知道该怎么解决
5. **不会的怎么办**：说清「我知道什么、不知道什么、会往哪个方向查」，别硬编
