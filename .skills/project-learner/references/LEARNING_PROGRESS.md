# 📚 Project Learning Progress — ZhuZhaoGUI (烛照)

> Last updated: 2026-09-20
> 当前预习中的域: D4 主窗口与布局体系（① 预习 2/5）
> 阶段进度: ①预习 17/48 · ②自建 0/48 · ③对照 0/48
> 三阶段完成: 0/48
>
> ⚠️ ②③ 两列记的是**考试分数**，不是"活儿有没有干"。D3 域的代码已落地（B3/B4/B5，commit `159bc2c`）、③ 对照也做过了，但**没有以考试形式考过**，故仍记 0/48。

## 学习阶段说明

同一个知识点会被学三遍，三遍的触发方式、考察对象都不同，因此**分数分开记**。

| 阶段 | 触发方式 | 考察对象 | 每轮做什么 | 难度 |
|------|---------|---------|-----------|------|
| **① 预习** | **默认，每个知识点都走** | 源工程 `ZhuZhao-V1.2.0` | 先上一节**预习课**（讲解 + 代码出处），再做一道**自测题** | 低 |
| **② 自建** | **仅用户主动要求** | 你自己的 `src/` | 考你自己写的代码 | 中 |
| **③ 对照** | **仅用户主动要求** | 两边同时 | 考差异与取舍 | 高 |

> **工作节奏**：以**知识域**为单位推进 —— 一个域内全部知识点预习完，再去实现这个域，
> 有空再回头做 ②③。②③ 不自动推进，由用户说了算。

> **记分规则**
> - 每列填该阶段的**历史最高分**，未学过填 `-`
> - `状态` 由**最新已完成阶段**的分数决定：有 ③ 看 ③，无 ③ 看 ②，无 ② 看 ①
> - 分档：`>= 7` ✅ 掌握 · `4 ~ 6` 🔶 学习中 · `<= 3` 🔴 薄弱 · 三列皆 `-` ⬜ 未学习

## Domain Summary

| 知识域 | 子知识点 | ①预习 | ②自建 | ③对照 | 最新均分 | 状态 |
|--------|----------|-------|-------|-------|----------|------|
| D1 工程组织与构建体系 | 5 | 5/5 | 0/5 | 0/5 | 4.7 | 🔶 学习中 |
| D2 观察者模式与单例 | 5 | 5/5 | 0/5 | 0/5 | 3.9 | 🔶 学习中 |
| D3 日志系统 | 5 | 5/5 | 0/5 | 0/5 | 3.4 | 🔶 学习中 |
| D4 主窗口与布局体系 | 5 | 2/5 | 0/5 | 0/5 | 2.8 | 🔴 薄弱 |
| D5 Qt 与 OpenCV 图像互通 | 4 | 0/4 | 0/4 | 0/4 | - | ⬜ 未学习 |
| D6 视觉窗口 QGraphicsView | 5 | 0/5 | 0/5 | 0/5 | - | ⬜ 未学习 |
| D7 缩略图列表与自绘控件 | 5 | 0/5 | 0/5 | 0/5 | - | ⬜ 未学习 |
| D8 多线程与界面刷新 | 5 | 0/5 | 0/5 | 0/5 | - | ⬜ 未学习 |
| D9 光度立体算法 | 5 | 0/5 | 0/5 | 0/5 | - | ⬜ 未学习 |
| D10 动态库开发与导出宏 | 4 | 0/4 | 0/4 | 0/4 | - | ⬜ 未学习 |

## Sub-topic Progress

### D1 工程组织与构建体系

| ID | 知识点 | ①预习 | ②自建 | ③对照 | 状态 |
|----|--------|-------|-------|-------|------|
| D1.1 | 目录层级与相对路径约定 | 3.5 | - | - | 🔶 学习中 |
| D1.2 | .pro 配置项全解 | 4.5 | - | - | 🔶 学习中 |
| D1.3 | qrc 资源系统与图标 | 5.5 | - | - | 🔶 学习中 |
| D1.4 | 国际化链路 tr/ts/qm | 5.5 | - | - | 🔶 学习中 |
| D1.5 | 双构建体系与前后端分离 | 4.5 | - | - | 🔶 学习中 |

### D2 观察者模式与单例

| ID | 知识点 | ①预习 | ②自建 | ③对照 | 状态 |
|----|--------|-------|-------|-------|------|
| D2.1 | ZZListener 抽象基类设计 | 3.5 | - | - | 🔶 学习中 |
| D2.2 | MESSAGE 枚举与位或组合 | 5.5 | - | - | 🔶 学习中 |
| D2.3 | ListenerManger 饿汉式单例 | 4.0 | - | - | 🔶 学习中 |
| D2.4 | registerMessage 位与拆包 | 4.0 | - | - | 🔶 学习中 |
| D2.5 | notify 查表广播与容器选择 | 2.5 | - | - | 🔴 薄弱 |

### D3 日志系统

| ID | 知识点 | ①预习 | ②自建 | ③对照 | 状态 |
|----|--------|-------|-------|-------|------|
| D3.1 | ZZLogMessage 懒汉单例与双检锁 | 4.5 | - | - | 🔶 学习中 |
| D3.2 | qInstallMessageHandler 接管机制 | 3.0 | - | - | 🔴 薄弱 |
| D3.3 | 日志宏封装与 QFATAL 签名错误 | 3.0 | - | - | 🔴 薄弱 |
| D3.4 | 日志落盘与文件轮转 | 3.0 | - | - | 🔶 学习中 |
| D3.5 | ZZLogWidget 与信号槽对接 | 3.5 | - | - | 🔶 学习中 |

### D4 主窗口与布局体系

| ID | 知识点 | ①预习 | ②自建 | ③对照 | 状态 |
|----|--------|-------|-------|-------|------|
| D4.1 | 手写布局 vs .ui 文件的取舍 | 3.0 | - | - | 🔴 薄弱 |
| D4.2 | 三层嵌套布局结构 | 2.5 | - | - | 🔴 薄弱 |
| D4.3 | QSplitter 与尺寸约束 | - | - | - | ⬜ 未学习 |
| D4.4 | setCentralWidget 与父子对象内存管理 | - | - | - | ⬜ 未学习 |
| D4.5 | 字体设置与 QSS 局部美化 | - | - | - | ⬜ 未学习 |

### D5 Qt 与 OpenCV 图像互通

| ID | 知识点 | ①预习 | ②自建 | ③对照 | 状态 |
|----|--------|-------|-------|-------|------|
| D5.1 | QImage 与 cv::Mat 内存布局差异 | - | - | - | ⬜ 未学习 |
| D5.2 | QImage2cvMat 格式分发 | - | - | - | ⬜ 未学习 |
| D5.3 | cvMat2QImage 与 clone/rb_swap | - | - | - | ⬜ 未学习 |
| D5.4 | 头文件非 inline 函数的 ODR 隐患 | - | - | - | ⬜ 未学习 |

### D6 视觉窗口 QGraphicsView

| ID | 知识点 | ①预习 | ②自建 | ③对照 | 状态 |
|----|--------|-------|-------|-------|------|
| D6.1 | Scene/View/Item 三者关系与坐标系 | - | - | - | ⬜ 未学习 |
| D6.2 | 滚轮缩放与缩放上下限 | - | - | - | ⬜ 未学习 |
| D6.3 | fitFrame 自适应比例计算 | - | - | - | ⬜ 未学习 |
| D6.4 | 鼠标坐标与像素取值 | - | - | - | ⬜ 未学习 |
| D6.5 | 棋盘格背景与 paintEvent 重写 | - | - | - | ⬜ 未学习 |

### D7 缩略图列表与自绘控件

| ID | 知识点 | ①预习 | ②自建 | ③对照 | 状态 |
|----|--------|-------|-------|-------|------|
| D7.1 | QListWidget 配置组合 | - | - | - | ⬜ 未学习 |
| D7.2 | setItemWidget 与 setSizeHint | - | - | - | ⬜ 未学习 |
| D7.3 | 横向与纵向列表的差异 | - | - | - | ⬜ 未学习 |
| D7.4 | currentRowChanged 与图像同步 | - | - | - | ⬜ 未学习 |
| D7.5 | LitImgItemWidget 的 paintEvent 自绘 | - | - | - | ⬜ 未学习 |

### D8 多线程与界面刷新

| ID | 知识点 | ①预习 | ②自建 | ③对照 | 状态 |
|----|--------|-------|-------|-------|------|
| D8.1 | QThread 两种用法对比 | - | - | - | ⬜ 未学习 |
| D8.2 | ZZProcessThread 为何不需要 Q_OBJECT | - | - | - | ⬜ 未学习 |
| D8.3 | 跨线程 finished 信号与 UI 刷新 | - | - | - | ⬜ 未学习 |
| D8.4 | 参数传递与结果取回设计 | - | - | - | ⬜ 未学习 |
| D8.5 | 共享数据的线程安全隐患 | - | - | - | ⬜ 未学习 |

### D9 光度立体算法

| ID | 知识点 | ①预习 | ②自建 | ③对照 | 状态 |
|----|--------|-------|-------|-------|------|
| D9.1 | 光度立体问题定义与光照方程 | - | - | - | ⬜ 未学习 |
| D9.2 | 由 Slant/Tilt 计算光源向量 | - | - | - | ⬜ 未学习 |
| D9.3 | SVD 求伪逆解 ρN | - | - | - | ⬜ 未学习 |
| D9.4 | 法向量归一化与 p/q 梯度场 | - | - | - | ⬜ 未学习 |
| D9.5 | 傅里叶域积分求高度场 | - | - | - | ⬜ 未学习 |

### D10 动态库开发与导出宏

| ID | 知识点 | ①预习 | ②自建 | ③对照 | 状态 |
|----|--------|-------|-------|-------|------|
| D10.1 | ZZ_API 与 ALGO_EXPORT 的来源 | - | - | - | ⬜ 未学习 |
| D10.2 | 导入库 .lib 与运行时 .dll 的分工 | - | - | - | ⬜ 未学习 |
| D10.3 | extern "C" 与 C++ 名字修饰 | - | - | - | ⬜ 未学习 |
| D10.4 | CMake 工程配置与输出目录 | - | - | - | ⬜ 未学习 |

## Detailed History

| # | Date | 阶段 | 知识点 ID | 知识点 | 问题 | 评分 | 追问轮数 | 薄弱点 |
|---|------|------|-----------|--------|------|------|----------|--------|
| 1 | 2026-09-13 | ① 预习 | D1.1 | 目录层级与相对路径约定 | `bin` 挪进 `src/ZhuZhaoGUI/` 后：两套构建系统各要改几处；是否会出现「能编过、能链接、但跑起来行为不对」 | 3.5 | 2 | ① 链接期（`.lib`）与运行期（`.dll`）混为一谈，误以为缺 `.dll` 就生成不了 exe；② 未答出「不报错但语义破坏」那一层；③【总结课答疑暴露】`set(VAR 值 CACHE PATH "doc")` 读不懂 —— 把文档字符串当成值；`-L` / `-l` 谁才是「库名」分不清；`.pro` 里重复的「4 处」说不出是哪 4 行。→ 下次重讲 D1.1 必须先铺 `set()` 与 `-L`/`-l` 的最小语法 |
| 2 | 2026-09-13 | ① 预习 | D1.2 | .pro 配置项全解 | ① 为什么 `INCLUDEPATH` 一条够、`LIBS` 必须分 Debug/Release，混链坏在哪一步；② 删掉 `CONFIG += c++17` 后还能不能用 C++17；③ `CONFIG` 与 `DEFINES` 各自说给谁听 | 4.5 | 2 | ① 混链 `.lib` 的根因说成「用到已经编译过的 lib」，未答到 **CRT 不匹配 / `LNK2038`**；② 把 `DEFINES += QT_DEPRECATED_WARNINGS` 的作用（弃用警告）错记到 `CONFIG += c++17` 头上 —— **相邻两行混为同一件事**；③ `CONFIG` vs `DEFINES` 的分工完全不知道。★本轮亮点：他**自己去翻文件**，准确定位到 `mkspecs/common/msvc-version.conf` |
| 3 | 2026-09-14 | ① 预习 | D1.3 | qrc 资源系统与图标 | ① 只把 exe 拷到别处，按钮图标还显示吗、为什么；② 写成 `QIcon("Resouce/icon/clear.png")`（无 `:/`）是编译期还是运行期出错 | 5.5 | 2 | ① 两题**结论都对**，但第 1 题理由说成「因为它是磁盘相对路径」——与现象矛盾（构建目录下并没有 `Resouce/` 却照样显示）；经追问 1 纠正到「虚拟文件夹」；② 第 2 题理由「它其实是编译器的」表述含混，**编译期/运行期各做了什么的边界**仍不牢；③ 追问 2 里把 `:/` 说成「exe 同目录下的虚拟文件夹」，经边界场景（改文件名 + 挪 U 盘 + 同名干扰文件）纠正为「注册在 exe 内部的虚拟资源系统」。★本轮亮点：追问后能自己说出「虚拟文件夹」这四个字，且 A/B 两问全对 |
| 4 | 2026-09-14 | ① 预习 | D1.4 | 国际化链路 tr/ts/qm | ① 把 `tr("Reset")` 改成 `tr("ResetButton")` 但**不重发 .qm**，按钮上显示什么、为什么不显示「重置」；② 把 `installTranslator` 挪到 `MainWindow w;` 之后会显示中文还是英文 | 5.5 | 2 | ① 第 2 题**满分**（英文 + 先装翻译器再构建界面）；② 追问 1 显示内容答「Reset」，漏了 "Button" —— 正解是 **source 原样返回 `ResetButton`**，且他一度以为是「因为有翻译器存在」才这样，未意识到**有/无翻译器时"查不到"的结果完全相同**；③ **`lupdate` 这个角色完全不知道**（追问 2 第 1 步答「不知道」）—— 链路最前端「谁生成 .ts」是盲区；④ 追问 2 漏了「**人工在 .ts 里填 translation**」这一步，以为 .ts 一生成就有中文，只提了 lrelease；⑤ ✅ 追问 2 第 4 步满分：答出「.qm 不会自动生效，要 .qrc 重新编译进 exe」，理由准确。★本轮亮点：追问 1 里自己推理出「映射的键值改了，所以会显示新英文」——**从现象反推机制**的能力开始成型。⑥【总结课答疑暴露】问「中文是我自己填的吗？是作者要先给翻译表吗？在哪？」——**「这个文件是谁给的、放在哪」类盲区再现**（他最大的卡点类型）。需记牢：`.ts` = 课程作者填完后只交付 `.qm`；`.ts` 在源工程 `src/ZhuZhaoGUI/language_ch.ts`（工程根，与 `.pro` 同级，不进 qrc）；`.qm` 在 `Resouce/translate/`；他的复刻工程**只有 .qm 没有 .ts** |
| 5 | 2026-09-14 | ① 预习 | D1.5 | 双构建体系与前后端分离 | ① 把算法库接口从 `vector<Mat>` 改成 `QList<QImage>` 会不会破坏前后端分离、后果是什么；② 阶段 E 编出新 `.dll`/`.lib` 后 `.pro` 要改什么、为什么产物必须落 `src/bin` | 4.5 | 2 | ① 第 1 题答得扎实：「导致前后端耦合，后端没法给其他项目调用」+「CMake 编译没有 Qt 库」两点都点在线上；② 第 2 题① 三段注释（`INCLUDEPATH` + Debug/Release 两个 `LIBS`）贴得准确；③ 追问 1 两问全对（链接期缺 `.lib` / 运行期缺 `.dll`），但"为什么必须落 src/bin"答成「exe 运行时路径检索简单」，机制含糊；④ ❗**追问 2 出现明确错误认知**：认为「dll 会找电脑**登记表（注册表）**」—— **DLL 加载完全不查注册表**，他疑似把 COM/ActiveX「组件需注册」的概念泛化到了 DLL 加载上；⑤ 追问 2 ① 没答（又贴了一行 CMake 的 `set(CMAKE_LIBRARY_OUTPUT_DIRECTORY_RELEASE ...)`），被"第几个去找"问法的题意绊住；⑥ 追问 2 ③ 答「不算吧，我又没指定」——结论接近（当前目录优先级很低）但理由错，需明确：**当前目录在搜索列表里，只是被 SafeDllSearchMode 排到了 System32/Windows 目录之后**。★本轮亮点：第 1 题的**设计意图**回答准确（分离的收益 = 后端可复用），说明"取舍"这一层他开始有感觉。⚠️ 下次复讲 D1.5 必须先用"多站搜索 vs 单站基准"的对照把 DLL 搜索顺序铺开，并明确否掉"注册表" |
| 6 | 2026-09-14 | ① 预习 | D2.1 | ZZListener 抽象基类设计 | ① `ZZListener oneListener;` / `new ZZListener` / `class MyL : public ZZListener {};` 三种写法各自的编译结果；② 为什么 `QVector<ZZListener*>` 能装 `MainWindow*`、`= 0` 在其中起什么作用 | 3.5 | 2 | ① ① 三问**一个编译结论都没给**，只在复述"这行在写什么"；c) 原样贴回等于没答；② 追问 1 卡死 —— 用「组织虚函数进行初始化啥的……我也忘了」蒙了一句，**vptr / 虚函数表这一层完全没有概念**，且没意识到编译器在抽象类这一步根本不看初始化；③ 追问 2 答「不能吧，这继承啥都没写啊，应该可以？」态 —— **不知道 `class MyL : public X {};` 本身合法（能编过）、`MyL m;` 才报错**，把"类抽象"与"实例化被禁"混为一谈；④ 把 `ZZListener` 说成单例（正解 `ListenerManger`），经一句提示才改对；⑤ `= 0` 的作用说成「强制要求对虚函数进行定义」，**主语错**（基类自己不定义，强制的是派生类）；⑥ 全程零 `file:line` 引用。★亮点：② 大题「基类指针装派生类对象是多态的体现」方向对；单例归属一点提示即自我纠正。⚠️ 下次复讲 D2.1 必须先用「抽象类禁的是**造对象**这个动作、不是 `new`」+「`MyL` 继承后**仍是抽象类**」两个反例开路，再铺对象内存布局与 vtable 最小模型；并强调 `error C2259 ... cannot instantiate abstract class` 这条诊断的 `instantiate` 一词 ⑦【总结课答疑暴露 → 触发整节重写】用户原话：「你预习课和总结课都要重写，我都不知道什么是抽象类，抽象类的一些独特规则，我完全理解不了你说的啥」—— **原版默认了他已掌握「多态 / 虚函数 / 实例化 / 指针与对象之别」，这是本课最大的教学缺陷**。→ 已重写两版：预习课改为「多态比喻 → virtual 开关 → `= 0` → 抽象类诞生」零基础推导链；总结课改为「抽象类规则全集（能/不能对照表）+ 本机 cl.exe 实测原话 + 判断流程图」。⚠️ **复讲 D2.1 一律从「什么是抽象类」起步，不许默认前置知识** ⑧【总结课答疑第二轮】问「（隐式 inline）inline 是什么」—— **C++ 关键字级概念仍是空白**，与他一贯的「这个符号是谁给的、从哪来」盲区同源。→ 已用本机 cl.exe 做 A/B 实测（`img.h` 里 `void f(){}` vs `inline void f(){}`，被 a.cpp/b.cpp 同时包含）：前者 `b.obj : error LNK2005: "void __cdecl f(void)" 已经在 a.obj 中定义`，后者 LNK2005 消失。**讲 D2/D3 域凡是出现「inline / static / extern / const / volatile / explicit」的位置，都要顺带交代一句它是什么、谁给的。** ⑨ 另需铺垫：include guard 防的是「同一 TU 重复贴」，防不了「两个 TU 各贴一份」 —— 该区别已在答疑中给出 |
| 7 | 2026-09-14 | ① 预习 | D2.2 | MESSAGE 枚举与位或组合传参 | ① `notify(ZHUZHAO_UPDATE_SRCIMAGE)`(=1) 会让 `MainWindow::RespondMessage` 哪几个分支执行、为什么；② 枚举改成 2 的幂后 `MainWindow.cpp:33-37` 一字不改，`m_messageToLister` 的 key 从什么变成什么、差异是好是坏 | 5.5 | 2 | ① ① **位与算得全对**（`1&1=1`、`1&2=0`、`1&4=0`）→ 位运算机制确实进去了；② **② 完全没答** —— 连答两句「不知道」「不知道」，并当场指出**题目缺陷**：「你问我代码相关要预习课或提问给出函数位置啊，我代码是老师代码是QT5我又不好编译，跳转也做不到。所以我不知道是那几个 key」→ **我出题时没把 `registerMessage` 的 4 个 if 贴进题目**，属**我方缺陷**，判定不计入他的扣分；③ 「坏事」主语没锁定 —— 没说清是"改前状态"是坏事还是"改前→改后差异"是坏事（正解：**改前状态是坏事（多了一个白捡的 key 3），差异本身是好事**）；④ 全程未打开源工程（他明说编不了、跳不了）。★两条硬教训已写进 `SKILL.md` 与用户级记忆：**(a) 提问/课文凡涉及代码，必须把那几行连同行号原样贴出，不许让他自己去翻源工程**；**(b) 不许默认他掌握 `enum` / 位运算 / `inline` / 多态 这类 C++ 概念**。⚠️ 复讲 D2.2 必须先把 `registerMessage` 全文（`ZZListener.cpp:32-65`）贴在题目里，再让他走那 4 个 if ⑧【答疑】两问：**(a)** 「这个就是观察者模式吗」—— 他把模式名误记成「**监管者模式**」，且不知道 GoF 角色与本工程的对应关系（作者把 `Observer` 改名成 `ListenerManger`，注释在 `ZZListener.h:3-17`）；**(b)** 「`notify` 是自定义的还是库函数？是库函数就列一下有哪些，我记进函数笔记」—— **「这个符号是谁给的」老卡点在 D2 域再次出现**。→ 已交付「GoF 角色 ↔ 本工程」对照图 + 本域**库函数清单**（只有 `QMap` / `QVector` 两个 Qt 头，及其 `find/end/iterator/value/operator[]/begin/push_back`）+ **自定义符号清单**（`MESSAGE`/`ZZListener`/`ListenerManger`/`RespondMessage`/`Instance`/`notify`/`registerMessage`/`mmap`/两个成员变量）。⚠️ **复讲 D2.2 / D2.3 / D2.4 都要先做一遍「自定义 vs Qt 库」分栏** |
| 8 | 2026-09-14 | ① 预习 | D2.3 | ListenerManger 饿汉式单例 | ① `Instance()` 只有 `return m_listenerManger;` 不判空不加锁，为什么安全、根据在哪；② 把 `ListenerManger() {};` 挪到 `public:` 后 `ListenerManger m2;` 能编过吗、会出什么看得见的问题；③ 删掉 `ZZListener.cpp:4` 只留头文件声明，报错在编译期还是链接期 | 4.0 | 2 | ① **③ 满分**：「链接期间就会报错，.cpp 函数找不到这个静态成员。因为 `static` 成员变量不占对象内存，编译器不知道把它的内存放哪儿」—— 理由引用准确；② **② 半对**：答出「能」「就不是单例了」✓，且**自主提到"浅拷贝会有多个索引指针"**（★加分：那正是本类没写 `= delete` 的真漏洞，已单独表扬）；但没答出"第二个对象有自己独立的 `m_messageToLister`、注册进去的人永远收不到广播"这一具体症状；③ **① 结论对、根据错**：说成「因为它是类的**私有变量**」—— `private` 是**访问权限**，挡的是别的代码，**挡不住别的线程**，与线程安全无关；④ ❗**定位错误：把饿汉式说成懒汉式**（"我们是懒汉式"）—— 本课标题级概念搞反，需重点复讲；⑤ ❗**明确暴露的空白：「我不知道啊，主要我不清楚它的启动流程」**（追问 2 问"那一刻有几个线程"）→ 这是他第一次**主动说出自己缺什么**，已作为总结课主线补上；⑥ 未给出具体行号（只说"ZZListener.cpp 正式生成"，含糊）。★亮点：③ 的理由引用、② 的浅拷贝联想。⚠️ 复讲 D2.3 必须先铺**程序启动时序表**（哪一步造 ListenerManger、哪一步才有第二个线程），再用"写它的时候有没有别的线程"这条线讲为什么不用锁；并当场纠正「饿汉/懒汉」两个名次 |
| 9 | 2026-09-14 | ① 预习 | D2.4 | registerMessage 位与拆包 | ① 重复注册后 `m_messageToLister[4]` 有几个元素、`RespondMessage` 被调几次；② 加第 5 个消息忘补 `if` 会怎样；③ 4 个 `if` 换成 `for` 循环，`RESET = 3` 的误注册会不会消失 | 4.0 | 2 | ① **②③ 全对**：②「能过，但无法触发这个状态，我们进行 SAVE 时会发现它没有用」= 抓住了「**静默失效**」；③「可以不会啊，这个 `(message & m) == m`，判断条件没有变啊」—— **理由完全准确**，是本域最漂亮的一句；② **① 没答上来**（先答"四个""调用一次"，追问 1 后回「我都没看懂你想考的是啥，问的东西我都没看懂」）→ ★**我方题目缺陷**：追问里提到「`notify` 内部那个 `while` 循环」却**没把 `notify` 的实现贴进题目**（违反当天刚落库的 hard rule），判定不计入扣分；③ 推断病因：把「消息编号是 4 / 有 4 个 if」与「名单里有几个元素」混了 —— **key 与 value 的元素个数**在他脑子里还没分家；④ 表述偏口语（"呆腻了"），但机制方向没跑偏。⚠️ 复讲 D2.4 必须把 **`registerMessage` 与 `notify` 两段代码一起贴出来**，再用三列表（步骤 / 关键行 / 名单里有几个）走一遍。★**用户主动要求把本课 ① 分从 6.0 下调到 4.0**（原话：「把 D2.4 改成 4 分吧，不然会让我产生误解」）—— 理由：① 未答、②③ 表述偏口语，6.0 会让他误判自己的水平。**这是首次用户主动下调自己的分**；处理：尊重他对自己记录的裁定，并在行内留痕（本条） |
| 10 | 2026-09-14 | ① 预习 | D2.5 | notify 查表广播与容器选择 | ① 名单改成 `QMap<int, ZZListener*>` 第一个出问题的地方；② `RespondMessage` 里加 3 秒循环后界面会怎样；③ `notify` 遍历中调 `registerMessage` 会怎样 | 2.5 | 2 | ① **三问全未答出**：① 答成反向质疑「监听者模式不就是要一个一个传吗，为什么要给一个 `ZZListener` 的数组」——**前提就错了：他以为"一条消息只对应一个监听者"**（正解：GoF 定义就是 one-to-many，数组用来防"后来者覆盖先到者"）；②③ 均答「不知道」；② 追问后情绪爆发：「为什么你会说 MainWindow 这一份？？我怎么知道？？？」→ **我方题目缺陷**：① 那问把"现状表 `4 → [MainWindow*]`"与"假想类型 `QMap<int, ZZListener*>`"塞进同一句，他无法同时拿住两个模型；**用"假设写法"出题时，必须先把假想出来的表单独画一遍**；③ 唯一亮点：他记住了表结构 `4 → [MainWindow*]` 并据此**主动反问"它有啥"**——其实是在要求定义。★★**本课真正的收获不在他的答案，而在收口前他爆发的那条批评**（见下） |
| 10b | 2026-09-14 | ① 预习 | **【教学事故记录 · 无分数】** | D2.5 收口前用户爆发 | — | 0 | — | 原话：「**我连现在讲哪个模块，那几个函数都不知道**！！notify 到底在哪里跑，它统治的是什么信息？我 key 是 runonce 跟这些有啥关系，**你怎么默认我什么都知道**，讲语法的时候那个 `&` 的语法又把我当弱智讲那么些」。**三条缺陷全在我方**：**(1) 连讲 D2.1~D2.5 五课，从未给过"模块地图"** —— 没交代过"观察者模块只有 `ZZListener.h`+`ZZListener.cpp` 两个文件、只有 3 个函数"，他在细节里迷了 4 小时；**(2) 详略分配反了** —— `&`/`0x` 这类支线掰开揉碎，主线（谁调用、跑在哪条线程、数据在哪）一句带过；这条他 2026-09-14 特别篇就说过（"细度要按是不是主线来分配"），**我违反了**；**(3) 使用未定义代词** —— "其余数据""自己想办法""这一份"。→ 三条已全部写成 `SKILL.md` 的 **hard rule**：**每课第一屏必须是模块地图（文件/定义了什么/谁调谁）**、**详略按主线分配（支线一句带过，除非他问）**、**禁止未定义代词**。★补救：本课已补 **RunOnce 五步调用链图** + 逐条回答他的三个问题。★长期：**以后每开一课，地图在前、细节在后** |
| 11 | 2026-09-15 | ① 预习 | D3.1 | ZZLogMessage 懒汉单例与双检锁 | ①源工程 `ZZLogMessage.cpp:110` 的局部 `QMutex` 能不能挡住两条线程、为什么；②挡不住的后果落在哪一行；③改成 `static QMutex muter;` 是否解决、为什么。追问1：`static` 局部变量建在哪个内存区 / 甲乙两写法下两线程是否同一份 / 为何乙能真互斥。追问2：B 走到 `:111` 当场发生什么、被放行后 `:113` 看到空还是非空、内层 `if` 到底防什么（含删掉内层 if 的反例推演） | 4.5 | 2 | ① **三问结论全对**（局部锁无效 / 会造两个 / static 能修），且两轮追问后未被推翻 —— 比 D2 那几课"追问即崩"是实质进步；② ❗**两处"结论对、机理错"**：把 `static QMutex muter;` 说成「在**堆**上创建」（正解：**静态存储区**，对照 `ZZLogMessage.h:32` 的静态成员声明与 `ZZLogMessage.cpp:105` 的定义；`new` 才产生堆内存，`:115` 造的对象本体才在堆）；把"B 醒来看到非空"的原因说成「因为它**被加锁了**」（正解：A 在 `:115` 已把 `m_pLogInstance` 填好，锁只负责**排队 + 放行**，不改任何变量的值）；③ 把 `:113` 内层 `if` 说成"查锁有没有被上"，正解是查 **`m_pLogInstance` 这个指针**（他追问2第3点自己说出了"两个线程可能同时进入上锁环节"——方向对，名词错位）；④ 全程零 `file:line`。★亮点：**自发想出"connect 连的是 A、发信的是 B"这个真实故障场景**（`ZZLogWidget.cpp:44`），是真的在脑子里跑了一遍运行期；追问1 三点全对（静态存储期 / 甲不是同一份乙是 / 只有一份）。【7.2 答疑新增】问「`connect` 是什么函数，我只知道 TCP 那个」→ **"同名不同物"盲区**：Qt `QObject::connect`（`QObject` 的静态成员函数，4 参数，第 1 个是 `QObject*`）vs socket `connect()`（`<sys/socket.h>` 全局函数，3 参数，第 1 个是 `int sockfd`）。已就地补充「补：Qt 的 connect 与 TCP 的 connect」一节（含 `signals:` / `Q_OBJECT` / `emit` 的最小交代），并**替换总结课第一节对应段落**；另按其要求把「主问题 + 两轮追问 + 总结课」**整节融合重写**成一节课。⚠️ **复讲 D3.1 以及讲 D3.5 / D4 / D6 / D7 时，凡出现 `connect` / `signals:` / `slots:` / `Q_OBJECT` / `emit` 都要先做一次「同名不同物 / 谁给的」交代**——这是他一贯的"这个符号是谁给的"盲区在 Qt 域的再现 |
| 12 | 2026-09-15 | ① 预习 | D3.2 | qInstallMessageHandler 接管机制 | ①`main.cpp:15` 执行后 `outputMessage` 被放到哪里、Qt 怎么找到它、何时调用；②`outputMessage` 为什么必须是自由函数、改成非静态成员函数会报什么性质的错；③`ZZLogMessage.cpp:17` 与 `:110` 两把锁各自保护什么、为什么两处都该是 static。追问1：地址具体存哪（翻 `ZZLogMessage.h:19-37` 确认类里有无成员能装）、真正的调用者是谁、装了 handler 后还有哪些日志会进来。追问2（反面场景）：注释掉 `installMessageHandler()` 后 `QDEBUG` 还会不会触发 `outputMessage`、日志去了哪、`outputMessage` 与 `QDEBUG` 之间是直接调用还是隔了中间人 | 3.0 | 2 | ① **② 答得漂亮**：「成员函数有 this 指针」「报函数类型不匹配」—— 直接命中 C++ 类型系统的本质，是本课唯一的高分点（仅错在"需要作为非静态成员函数或 static"方向说反，改成"需要**不需要 this 就能调**的那种函数"即对）；② ❗**本课主线（接管机制）整块空着**：三次尝试 `QString logPath();`（末尾带括号是**函数声明**，不是变量）、`QMessageLogContext`（是 `ZZLogMessage.cpp:15` 的参数类型）都不成立；追问 1 提示后他确认"类里没有成员能装函数地址"，**这一步对了但没往下推** —— 正解是地址存在 **Qt 库内部的全局函数指针变量**里（QtCore 静态数据区，根本不在本工程），`ZZLogMessage` 只是"递地址的人"；③ 把调用者说成「在 QDEBUG 的时候会执行」—— 实际隔了两层：`QDEBUG`(宏) → `qDebug()`(Qt 函数) → Qt 内部检查全局 handler → 非空才调 `outputMessage`；④ 说默认输出是「QT 自己的界面」—— **Qt 没有日志界面**，`ZZLogWidget` 是本工程自己写的 `QWidget`；不装 handler 时日志走 `OutputDebugString` / `stderr`（调试器输出窗口），界面上永远看不到；⑤ **漏答"装 handler 的最大收益"**：Qt 框架自身产生的日志（QSS 解析失败、QLayout 重复设置、跨线程动定时器）也会流进来，这是实用价值最高的一点；⑥ ③ 把锁当成了被保护对象（「`:17` 保护了锁不会随函数结束而销毁」）—— **完全没答"保护什么"**：`:17` 保护的是 `:76-81` 那个**磁盘日志文件**，`:110` 保护的是 `m_pLogInstance` 指针的"检查 + new + 赋值"三步；⑦ **上一课刚纠正过的点退回去了**：落点又在"活得久"（生命周期）而非"只有一份"（唯一性）。★两轮追问里唯一答到点的是「中间人就是 QT 自己」。⚠️ **复讲 D3.2 必须从"地址存哪 / 谁调谁"这条链的图起步**，并用反面场景（注释掉 `installMessageHandler`）当脚手架；讲 D3.5 / D4.5（QSS）时要把"Qt 框架自身日志也会流进 handler"这条实际收益兑现出来 |
| 13 | 2026-09-15 | ① 预习 | D3.3 | 日志宏封装与 QFATAL 签名错误 | ①`__FUNCTION__` / `__LINE__` 的值分别是谁、在哪个阶段填进去的，为什么不能用 `MyLog()` 函数代替；②四个宏里哪个一写就编译失败、为什么；③宏末尾那个分号会带来什么麻烦、为什么源工程没暴露。追问1：`void << "文字"` 是否合法、QFATAL 崩的根因、为什么不能用函数 | 3.0 | 2 | ① **① 前半答对了本课最容易混的一格**：「`__LINE__` = 当前行号」+「预处理期 / 编译器」的阶段归属**都是他自己给出的** —— 说明"宏是文本替换"这件事已理解到位；② ❗ ① 后半（为什么不能用函数代替）完全没答；② ❗ **② 完全空白**（这是本课的标题知识点），追问后仍答"不知道"；③ ③ 找到了正确位置（「`QDEBUG("保存成功")` 内部有个 `;`」）但**没推出后果**（`if`/`else` 报 `error C2181`），也没答"源工程两处为什么没暴露"（两处都是**裸写一行**，外面没有 if）；④ 追问 1 第 1 问「左操作数」他明确说"不清楚"→ 已改为直接讲解；⑤ 全程零 `file:line`。★亮点：① 的阶段区分准确 —— **这是他连续三课里唯一一次自己说对"谁在哪个阶段做了什么"**。【7.2 答疑三轮，全部为他主动追问】**(a)** 「`qFatal` 为什么是 void，不都是日志吗，它为什么这么特殊」→ 已讲透（四个函数是**严重程度递进的标尺**；**返回值是给调用者的回执**，而 `qFatal` 打完就 `abort()` 不回调用者；流式"攒到语句末才行动"与"当场终止"**机制上打架**；`Q_NORETURN` 能抑制 `warning C4715`；Qt4→Qt5 演进史）**(b)** 「宏竟然可以传值 message」→ **用词纠正：宏参数不是"传值"是"抄字"**（给了 `SQUARE(i++)` 双自增的证据；并指出 `QDEBUG` 与 `qDebug` 之间**没有任何绑定关系**，只是替换文本里恰好写了那七个字母；顺带 `QDEBUG("%d",x)` 会报 **C4002 实参太多**）**(c)** 「`__LINE__`/`__FUNCTION__` 到底是什么、什么叫编译期替换成字符串字面量、`__LINE__` 是谁的行号」→ 已讲透（**占位符 = 抹掉重写**；`__LINE__` 数的是**它被抄过去之后实际站的那一行**；**"函数是我过去，宏是你过来"**）。**产出**：按他要求把三轮答疑**全部融进总结课正文**（不新增"补：xxx"段落），并按他给的 SVG 代码块格式交付了完整重写版；顺带修正原稿两处数字错误（`__LINE__` 是 8 个字符不是 7，`__FUNCTION__` 是 12 个不是 13）。⚠️ **复讲 D3.3 必须先讲"预处理器阶段到底做了哪三件事"（换宏名 / 抄实参 / 填 `__LINE__`），再讲编译器填 `__FUNCTION__`** —— 他这次卡在"替换"这个词的指代上，说明**"编译阶段分工"每课都要点名** |
| 14 | 2026-09-15 | **跳过自测，直接讲课** | D3.4 | 日志落盘与文件轮转 | — | — | — | **【教学事故记录 · 无分数】本课连出三次事故，全部在我方**：**(1)** 预习课里用了「当前工作目录」这个词却**没定义它**就直接出题 → 他答不出来，原话「我其实不知道，你预习课有讲吗，我真不知道啊，能不能预习课先讲再问我，**我学习效率很低啊，我都听不懂**」；**(2)** 出的题要求说出**具体路径**，而预习课只给抽象描述（"三种启动三个地方"），**一个路径都没写** → 他当场抓到「**为什么你都不点名**」；**(3)** 预习课里写错一句「你写的 `MainWindow.cpp` 已经用过 `QCoreApplication::applicationDirPath()`」—— 实际他的 `MainWindow.cpp` 还是骨架，源工程整个 `src` 里 `applicationDirPath` **零出现**（grep 实测），已在追问中认错更正。**收口方式**：他直接叫停 ——「**你直接给总结课吧，我不知道你再讲什么鸟事，预习课总结课都重讲**」→ 已交付**合并版一课**（预习+总结一遍讲完，无自测、无追问、不打分）。**技术要点**：`$$PWD`（qmake，①②构建期）vs 当前工作目录（操作系统，③运行期）—— 他原以为 `$$PWD` 属于"预处理"，已纠正 qmake 阶段**更早**；`QDir(...)` 本身不碰磁盘（只有 `:135` 的 `exists()` 与 `:137` 的 `mkpath()` 碰磁盘）；`.arg()` 占位符替换（他第一次见）；点名了具体落点（Qt Creator 运行 → `...\build\Desktop_Qt_6_11_2_MSVC2022_64bit_Debug\log\2026-09-15\log.txt`；双击 exe → `...\src\bin\log\2026-09-15\log.txt`）。**新增四条教学约定（已进 `SKILL.md`）**：①跨领域概念（操作系统/构建工具）必须自带定义+具体实例；②凡涉及路径必须打印**具体绝对路径**并点名他机器上的真实路径；③出题前先给**同类型样例题+完整答案+推导过程**（他的原话："就像数学课上老师先讲例题，再留一道让你自己做"）；④**不许考"预习课没以具体形式讲过的信息"**。**【课后追问 · 讲透后才收口】** 他连问两个"点名级"问题：**(a)**「`QString("%1/log_%2.txt").arg(...).arg(...)` 这行在写什么」→ 讲了 `.arg()` 占位符替换（编号顺序=填空顺序，不能颠倒），并点出 `:94` 的**双斜杠**脏点（`logPath()` 返回值末尾自带 `/`，格式串里又写了一个）；**(b)**「`log/2026-09-15/` 到底建在哪个位置、和谁同级」→ **必须给出绝对路径和同级对照物**才算答完（双击 exe → 建在 `D:\SmartGit\workspace_git\zhuzhaoGUI\src\bin\log\`，与 `images\`、`ZhuZhaoGUI.exe` **同级**；Qt Creator 运行 → 建在 `...\build\Desktop_Qt_6_11_2_MSVC2022_64bit_Debug\log\`，与 `Makefile.Debug` 同级）。**结论：他问"在哪"时必须点名到具体路径 + 明确指出同级的东西，抽象描述一律不合格。**
**【课后追问 2】** 问「`logfile.size() < 4` 里的 `4` 到底是什么」→ 讲清：**魔数、源码无注释、作者意图不可考**（只能推测，不许断言）；实际效果 = 把 0~3 字节的文件也算作"空位"；由于正常日志至少几十字节，**它和写 `1` 没有实际区别，阈值选得很随意**；**真正的问题是"用 `size()` 这个近似值去间接推断文件是否存在"，而这件事有专门的正规 API `QFile::exists()`**；并**推演**出一个边界缺陷（若存在 1~3 字节的 `log_N.txt`，`QFile::rename()` 因目标已存在而返回 `false`，而代码 `:98-99` 不看返回值直接 `return` → **轮转静默卡死**，`log.txt` 无限增长）。**该推演已标注"未实测"**。他的收口：「行吧，看来我后续改写需要把这个代码小修一下」。
⚠️ **复讲 D3.4 一律按"合并版一课"讲，不再单独出题**；讲时**必须**包含：绝对路径点名、当前工作目录的定义与"由启动者设定"、`$$PWD`（qmake/构建期）与工作目录（操作系统/运行期）的阶段区分、`4` 是魔数这件事 |
| 15 | 2026-09-15 | ① 预习 | D3.5 | ZZLogWidget 与信号槽对接 | ①`ZZLogWidget.cpp:44` 那条 `connect` 的四方分别是谁、`sigDebugHtmlData` 与 `QTextBrowser::append` 各是谁定义的；②工作线程 emit 之后 `append()` 在哪条线程执行、为什么不会因为跨线程出错；③`ZZLogMessage.cpp:83` 的 `emit sigDebugStrData` 究竟发生了什么、算不算问题。追问1：`m_pLogTextBrowser` 在哪一行创建、parent 是谁 → `ZZLogWidget` 谁创建 → `MainWindow` 谁创建 → 它属于哪条线程。追问2：`ZZLogWidget.h:25` 那一行执行后内存里发生什么、真正创建对象的那一行、若 `ZZLogWidget` 在工作线程 `new` 则走 Direct 还是 Queued。★**本课首次启用「先给同类型样例题 + 完整答案 + 三步口诀，再出真题」的新流程** | 3.5 | 2 | ① **① 的四方全对**（谁发/发什么/谁收/收下干什么），`QTextBrowser::append` 是 Qt 自带也判对；② **③ 方向对**：「可能 `sigDebugStrData` 根本用不到」= 抓到了死信号；③ ★**追问1 第 2、3 问全对**（`MainWindow.cpp:56` 的 `new ZZLogWidget(this)`、`main.cpp:23` 的 `MainWindow w;`），并**自己指出 `this` 就是 parent** —— 这是这条链上最关键的一扣；④ 第 4 问**结论对**（主线程）。❗ **但卡在两个点上**：**(a) 声明 ≠ 创建** —— 他把 `ZZLogWidget.h:25` 的 `QTextBrowser* m_pLogTextBrowser;` 当成"创建对象"（那一行只有类型+名字+分号，**没有 `new`**），真正的对象在 `ZZLogWidget.cpp:37` 的 `new QTextBrowser(this)` 造出来；**(b) 误以为存在「日志线程」** —— 他问「`m_pLogTextBrowser` 属于日志线程吗」，实际上 **`outputMessage` 在谁打日志就在谁的线程跑**，工程里**根本不存在专门的日志线程**；⑤ 追问2 三问全部答"不知道"，并出现明显情绪波动（「我是蠢比，这些问题我都不知道」）→ **已改为直接超细致讲解，不再追问**。⚠️ **两条必须长期带走的**：**(1) 每个 QObject 都记着"自己所属线程"，这个值在【对象被创建的那一刻】定下来**；**(2) AutoConnection 的精髓一句话：槽函数永远在"接收者所属的那条线程"执行** —— 同线程=直接调用，不同线程=排队到接收者线程。⚠️ **复讲 D3.5 要先用"指针格子 vs 对象本体"的图把「声明 / 初始化 / 创建」三段分开**，再讲线程归属 |
| 16 | 2026-09-15 | ① 预习 | D3.4 | 日志落盘与文件轮转（**补录评分**） | 用户事后要求「D3.4 怎么没成绩，你查查记录给个分」→ 按他当时的作答补录。**原作答**：①「当前目录下的 log_1 文件夹，会（落在同一处）」②「`file.close()` 那 file 还能被访问吗，其次会不会关闭前会再往里面塞什么东西」③「那会崩溃啊，限定死了就 99，所以代码其实有问题」。追问里 `$$PWD` 答成「预处理会调用」 | 3.0 | 1（未完成） | ① ✅ ① 里「**当前目录下的**」这个判断是对的 —— 相对路径的基准确实是当前目录；✅ ③ 抓到了「限定死了就 99，代码有问题」这个兜底缺失。② ❌ ① 的三处错：把日期目录说成 `log_1`（`log_1` 是轮转文件名 `log_1.txt`，`logPath()` 建的是 `log/2026-09-15/`）、答「会落在同一处」（实际**不会**，工作目录随启动方式变）。③ ❌ ③ 答「会崩溃」—— 实际是**静默失效**（循环跑完什么都不做，日志无限增长），**崩溃和静默失效是两回事**，他在 D2.4 答对过"静默失效"这个概念，这次没用上。④ ❌ `$$PWD` 说成「预处理」—— 那是 **qmake**（①构建期），比 C++ 预处理器（②编译期）更早。⑤ ② 抛出的**两个疑问方向都对**（"close 后还能访问吗" = 怀疑 size 是否有效；"关闭前会不会还有东西要塞" = 怀疑缓冲顺序），但没形成答案。**评分**：准确性 3 / 深度 3 / 代码关联 3.5（引用了真实代码但零行号）/ 设计思维 3 → **3.0**。⚠️ **重要备注**：① 的失分**部分属于题目缺陷** —— 我在预习课里用了「当前工作目录」却没定义它、要求说出具体路径却一个路径都没写（已被他抓到「为什么你都不点名」）。按 2026-09-15 新增的硬规则「不许考预习课没以具体形式讲过的信息」，**① 这一问本不该出**。本次 3.0 已包含对这一点的补偿，故不再下调；**复讲 D3.4 后应就①单独重考** |
| 17 | 2026-09-17 | ① 预习 | D4.1 | 手写布局 vs .ui 文件的取舍 | ①Designer 四环（输入文件 / 工具 / 生成物 / 使用者）在源工程 `ZhuZhao-V1.2.0` 里**分别存在还是不存在**，每环给一个一眼判定的依据；②`MainWindow.h:8` 的 `namespace Ui { class MainWindow; }` 是谁写进去的、现在还有没有作用、凭什么这么讲；③改走 Designer 路线后，「哪一件事会和现有代码打架」「哪一件事 Designer 根本给不了」，分别指什么。**追问1**：(a) `.pro` 里 `FORMS += mainwindow.ui` 这一行是谁、在什么时候写进去的／不写它 uic 还会不会被启动；(b) 这条链在源工程里接上没有，给一个能一眼判定的具体位置。**追问2**（把源工程 `.pro` 全文 81 行贴进题目后）：(i) 管 uic 的变量出现了吗、行号；(ii) 用 `SOURCES` 证明"接上了"成立吗、为什么 `SOURCES` 证明不了 uic 有没有被启动；(iii) `Ui::MainWindow` 这个类实际存在于哪个文件、那个文件存在吗 | 3.0 | 2 | ① ❗**三轮都没答到"落到本工程"这一层**：Round 0 只复述了链子的形状（「Designer 是图形化界面 / `.ui` 是 XML，类似 HTML 前端可视化代码 / 编译时生成对应 `.h` / 生成 `ui_mainwindow.h`」），**四环在源工程里存不存在一个字没提**，②③ 完全空白。② ❗❗**判定题答反 + 定位错文件 + 定位错变量（本课最严重的一处）**：追问1(b) 他贴的是**他自己工程** `D:\SmartGit\workspace_git\zhuzhaoGUI\src\ZhuZhaoGUI\ZhuZhaoGUI.pro:32-37` 的 `SOURCES` 段，结论「接上了」；**正解是没接上**，判据是源工程 `.pro` 里**没有 `FORMS`**。`SOURCES` 只决定哪些 `.cpp` 交给 `cl.exe`，与 uic 分属两条独立链，`SOURCES` 里也根本没有 `.ui`。③ ❗❗**「同名不同物」盲区第三次现形**（前两次：D3.1 的 Qt `connect` vs socket `connect()`；Qt Designer 里 `text` 属性 vs `objectName` 属性）：追问2(iii) 问 `Ui::MainWindow` 存在于哪个文件，他答的是 `MainWindow` 这个类（贴了他自己 `MainWindow.h` 的 `class MainWindow : public QMainWindow { Q_OBJECT ... ZZLogWidget* m_pLogWidget; };`）——把 **`::Ui::MainWindow`**（命名空间 `Ui` 里那个）和**全局的 `MainWindow`** 当成同一个类。两个类**只重名，没有任何继承或包含关系**。④ 术语：把 `ui_mainwindow.h` 说成「便于 **.c** 文件引用」——本工程 0 个 `.c`；C++ 源文件是 `.cpp`、头文件是 `.h`。⑤ 亮点：追问1(a)「不会，不引入就不会被启动」结论对；追问2(i)「没有出现」对；追问2(ii)「不成立，因为没有 `.ui` 的路径」——**这个理由成立**（`SOURCES` 里一个 `.ui` 都没有，看不出 uic 跑没跑），只是没点透"两者是两条独立链"。⑥ 全程**零源工程行号**；两次贴代码贴的都是自己工程的文件。⚠️ **复讲 D4.1 必须**：先立「`.pro` = 一张输入清单表，四类输入 = 四条独立链，每条链一个工具一个产物」这张表，再点明 **`FORMS` 是 uic 的唯一开关**；并且**在讲 uic 生成物的同时，就把 `MainWindow`（源工程 `MainWindow.h:17-41`）与 `Ui::MainWindow`（生成物 `ui_mainwindow.h:55-57`）两个类的定义位置并排摆出来**，因为他会把这两个重名的类合并成一个。⚠️ **评分说明**：Round 0 的 ①③ 整块空 = 主线未答，② 的定位题答反，故四维给 3.0 / 3.0 / 3.0 / 2.5 → **3.0**。**不得因为"链子形状讲对了"而抬高**——那一段是预习课刚讲过的复述。 |
| 18 | 2026-09-20 | ① 预习 | D4.2 | 三层嵌套布局结构 | 源工程 `MainWindow.cpp:70-97` 里 8 次 `addWidget` 三问：**(i)** 哪几次点的左边是 `QSplitter`、哪几次是布局 + 两拨行为差在哪；**(ii)** `:83` 为什么必须用 `addLayout` 而不是 `addWidget`；**(iii)** `:84-85` 两行为什么必须存在，为什么 `:71`、`:72` 那边不需要同样的壳 | 2.5 | 0/2（提前收口） | ① **他明确说"我不知道，说实话"，按 2026-09-15 立的规矩（连续答不出立即改直接讲解、不再追问）当场收口，本课追问轮数记 0。** ② ✅ **他自己给出的高层模型基本是对的**：「要有 widget 然后用布局来排版 widget，这样他和他的继承格式不会乱」—— 这句话抓住了"控件是盒子、布局负责排"的骨架。③ ✅ 引用了 `:87-89` 并说出 splitter 的**功能**（"起到分割显示窗口和 log 日志窗口的作用"）—— 方向对，但**分错了对象**：真正分「配置面板/日志面板」的是 `:71-72` 的 `pLeftSplitter`；`:87-89` 的 `pMianSplitter` 分的是"左侧纵向块"和"右侧显示块"。④ ❗❗**本课唯一的真问题，也是他卡住的全部原因：他看不懂代码为什么"一会儿 widget 一会儿 layout"** —— 原话「pViewLayout 是布局啊，你 addWidget 是啥意思」「这个一长串我不知道他为什么一会 widget 一会 layout」。**他把"布局自己可以 addWidget 控件"和"控件不能 addWidget 布局进分隔条"这两件事混成了一团。** ⑤ ❗ 没定位到函数归属（(i) 没回答"哪几次左边是 splitter"），全程未说出任何一个 `addWidget` 的**归属类名**。⑥ **根因判断**：D4.2 预习课里那节「布局世界里只有两种东西 —— 控件 vs 布局」（含那个 3 列表格）没落地；他记住了"控件+布局"这两个词，但**没建立"造布局 → 装满 → 最后找宿主挂上（`setLayout` 或 `addLayout`）"这个动作序列**。⚠️ **复讲 D4.2 必须**：先立**动作序列模型**（①造宿主控件 ②造布局 ③往布局里放东西 ④把布局挂到宿主上），再给 **5 行"装入方式对照表"**（控件→分隔条 = `QSplitter::addWidget`；控件→布局 = `QLayout::addWidget`；**布局→布局 = `addLayout`**；**布局→控件 = `setLayout`**；控件→主窗口中央 = `setCentralWidget`），最后用一棵「每条边都标上用什么函数装进去」的树收口。**不许只给"控件 vs 布局"的概念对举** —— 他要的是"每一步谁对谁做什么动作"。 |
